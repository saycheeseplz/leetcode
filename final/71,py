class Solution:
    def simplifyPath(self, path: str) -> str:
        stack = []
        for chunk in path.split("/"):
            if chunk == "" or chunk == ".":
                continue
            elif chunk == "..":
                if stack:
                    stack.pop()
            else:
                stack.append(chunk)
        return "/" + "/".join(stack)